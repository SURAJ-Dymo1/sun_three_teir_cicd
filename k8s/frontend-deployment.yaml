apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: surajmane121045/sun_frontend_app:latest
        ports:
        - containerPort: 80

        env:
        # ConfigMap values
        - name: VITE_Backend_API_URL
          valueFrom:
            configMapKeyRef:
              name: all_config
              key: VITE_Backend_API_URL